name: Legacy Gemini Art Generation Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: legacy-gemini-art-disabled
  cancel-in-progress: false

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Disabled
        run: echo "Legacy Gemini generation is disabled. Use the N100 Codex worker pipeline."
